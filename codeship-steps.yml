- type: serial
  name: Tests
  steps:
    - service: application
      name: Run linter
      command: yarn run lint
    - service: application
      name: Run tests
      command: bash -c 'wait && yarn run test'